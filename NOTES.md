# Catatan Week 1

> Tuliskan apapun yang kalian pelajari pada phase 2 week-1 di file ini.
>
> "Pemahaman yang baik berasal dari keinginan untuk terus belajar, dan catatan adalah langkah pertama menuju pengetahuan yang dalam."

install bcryptjs (https://www.npmjs.com/package/bcryptjs) - npm install bcryptjs
install jwt (https://www.npmjs.com/package/jsonwebtoken) - npm install jsonwebtoken
install .env (https://www.npmjs.com/package/jsonwebtoken) npm i dotenv --save

install axios (https://axios.rest/pages/getting-started/first-steps.html) - npm install axios
install multer (https://www.npmjs.com/package/multer) - npm install multer ==> cari MemoryStorage, jgan lupa const multer = require("multer");

install jest (https://jestjs.io/docs/getting-started) - npm install --save-dev jest
install supertest (https://www.npmjs.com/package/supertest) - npm install supertest --save-dev